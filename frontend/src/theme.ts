import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#27374D'
    },
    background: {
      default: '#DDE6ED'
    },
    secondary: {
      main: '#9DB2BF'
    }
  }
})

export default theme
